# ملاحظة الإصلاحات — (1) حفظ لون الخط للعضو المسجل + (2) ألوان العضويات في ورقة المستخدم

تم تطبيق تعديلين على المشروع. الخادم يعمل ويطبّقهما.

---

## ① حفظ لون الخط المخصص للعضو المسجل (يسري من أي جهاز)

**المشكلة:** كان اللون يُحفظ في `localStorage` للمتصفح فقط، فيضيع عند الدخول من جهاز آخر.

**الحل:** إضافة عمود `color` في جدول `users` وحفظه في الحساب، ثم تحميله تلقائياً عند كل دخول.

### الملفات المعدّلة
| الملف | التغيير |
|---|---|
| `database.js` | إضافة العمود: `ALTER TABLE users ADD COLUMN color TEXT DEFAULT ''` (تلقائي عند التشغيل). |
| `server.js` | 
| | — إضافة `color` إلى دالة `pubUser` (فتظهر في بيانات المستخدم والرسائل). |
| | — مسار جديد `POST /api/color` لحفظ اللون (للأعضاء المسجّلين فقط). |
| | — في معالجة رسالة العام: إذا لم يُرسل العميل لوناً، يُستخدم اللون المحفوظ في حساب العضو. |
| `public/js/app.js` | 
| | — دالة `syncMyColorFromProfile()` تُحمّل اللون المحفوظ من الحساب عند الدخول وعند تعديل الحساب. |
| | — عند اختيار لون من قائمة الألوان يُحفظ في `/api/color` (للأعضاء المسجّلين). |

**النتيجة:** عضو مسجّل يختار لوناً → يُحفظ في الحساب → يدخل من أي جهاز آخر بالاسم المسجّل نفسه → يجد نفس اللون عند الكتابة. (تم التحقق: دخول جديد على «جهاز آخر» أعاد اللون نفسه.)

---

## ② ألوان العضويات في ورقة المستخدم (كل عضوية بلونها)

**المشكلة:** في نافذة المستخدم، كانت تظهر «سوبر ادمين» ببنفسجي (`#7c3aed`) بدل الأسود، والألوان لم تكن مطابقة لألوان الأسماء في العام.

**الحل:** توحيد اللون حسب **نفس لون الاسم في العام** (`userColor`):
- **سوبر ادمين / سوبر ماستر / ادمن → أسود**
- أدمن غرفة → أحمر
- مميّز (`mmez`) → زهري
- VIP → أزرق
- بلس → أخضر
- بريميوم → أزرق فاتح
- مسجّل بدون عضوية → بني
- زائر → أسود

### الملفات المعدّلة (`public/js/app.js`)
- `userSheetMembershipColor()` → تعيد الآن `userColor(u)`.
- شارة/نص العضوية في الملف الشخصي (`openProfile`) → تستخدم `userColor(u)`.

---

## ③ لوحة المفاتيح على الجوال (سلوك كالتطبيقات)

**المشكلة:** عند فتح الصفحة من الهاتف والضغط على حقل الكتابة، لم تكن لوحة المفاتيح تُفتح بشكل متناسب، وقد يُغطّى حقل الكتابة أو تزيح الرسائل.

**الحل:** جعل الإطار يتقلّص إلى مساحة الرؤية الفعلية (فوق لوحة المفاتيح) عند فتحها، وإبقاء الحقل النشط ظاهراً ومُمرّراً إلى مجال الرؤية — مثل تطبيقات الجوال.

### التغييرات
| الملف | التغيير |
|---|---|
| `public/index.html` | تحديث `viewport` meta بإضافة `viewport-fit=cover` و`interactive-widget=resizes-content` (على أندرويد تجعل المحتوى يتقلّص فوق الكيبورد كالتطبيقات). |
| `public/js/app.js` | معالج `mobileKeyboardAdapter` يستخدم `visualViewport`: يُضبط ارتفاع `#frame` ليطابق مساحة الرؤية الفعلية، ويُمرّر الحقل النشط (إدخال/نص) إلى مجال الرؤية، ويُنزل منطقة الرسائل لآخر رسالة عند فتح/إغلاق الكيبورد. |

**النتيجة:** عند الضغط على حقل الكتابة من الهاتف تُفتح لوحة المفاتيح ويبقى الحقل والرسائل الأخيرة ظاهرين فوقها، ويُعاد ضبط الحجم عند الإغلاق. (تم التحقق باختبار حقيقي بنافذة بحجم هاتف: الإطار يتّبع ارتفاع مساحة الرؤية ويُبقي الحقل ظاهراً.)

---

---

## ⑤ إخفاء شريط الكلمات المقترحة فوق الكيبورد

**المشكلة:** عند الكتابة من الهاتف، كان يظهر شريط اقتراحات/تصحيح تلقائي بين الدردشة والكيبورد.

**الحل:** إضافة خصائص على حقل الرسالة العامة `msgInput` وحقل الرسالة الخاصة `pmInput` (في `public/index.html`) تمنع المتصفح من إظهار شريط الكلمات المقترحة/التصحيح التلقائي:
```html
autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false"
```
- `autocorrect="off"` → يعطّل التصحيح التلقائي وشريط الاقتراحات (خصوصاً على iOS).
- `autocomplete="off"` → يمنع الإكمال التلقائي.
- `autocapitalize="off"` → يمنع تكبير الحرف الأول.
- `spellcheck="false"` → يمنع التدقيق الإملائي.

**النتيجة:** عند الكتابة يبقى الكيبورد والدردشة مباشرة دون شريط مقترحات فوق الكيبورد.

---

## ⑧ إصلاح "الفراغ/التمدد" عند فتح الكيبورد (الشاشة تصبح فارغة)

**المشكلة:** بعد تفعيل `VirtualKeyboard API`، كان الإطار يرتفع فوق الكيبورد لكن منطقة الرسائل كانت **تتمدد وتبدو فارغة** (مسافة بيضاء كبيرة).

**السبب الجذري:** منطقة الرسائل `.c-msgs` تستخدم `flex:1; overflow-y:auto` لكن **بدون `min-height:0`**. في تخطيط flex-column لا يسمح العنصر لنفسه بالانكماش تحت ارتفاع محتواه، فمنع المنطقة من الانكماش عند تقلّص الإطار فتظهر ممتدة/فارغة.

**الحل:**
- `public/css/style.css`: إضافة `min-height: 0` إلى `.c-msgs` ليتقلّص بشكل صحيح مع ارتفاع الإطار.
- `public/js/app.js`: تحسين حساب الارتفاع المتاح بأخذ **الأصغر** بين `window.innerHeight - height` وبين ارتفاع `visualViewport` — كي لا يُطرح الارتفاع مرتين في المتصفحات التي تُقلّص `window.innerHeight` تلقائياً.

**التأكيد (اختبار حقيقي):** عند فتح كيبورد بارتفاع 320:
- الإطار: **844 → 524** ✓
- منطقة الرسائل انكمشت **626 → 306** (بدل التمدد) ✓
- الرسائل ما زالت ظاهرة (`lastMsgVisible=true`) ✓
- لا أخطاء في الصفحة ✓

---

## ⑦ تحسين فتح الكيبورد على الدردشة (VirtualKeyboard API)

**المشكلة:** طريقة `visualViewport` السابقة لم تكن تحل مشكلة "شريط الأدوات/الاقتراحات" أو ضبط الارتفاع بشكل موثوق على كل الأجهزة.

**الحل (في `public/js/app.js` — `mobileKeyboardAdapter`):** استخدام **`navigator.virtualKeyboard`** مع `overlaysContent = true` وحدث `geometrychange` (الأسلوب الأكثر موثوقية)، مع بقاء `visualViewport` كمسار احتياطي:
- عند التهيئة: `VK.overlaysContent = true`.
- مستمع `geometrychange`: يقرأ `boundingRect.height` (ارتفاع الكيبورد) ويضبط ارتفاع `#frame` إلى `window.innerHeight - height`. عند `height === 0` يُعاد الإطار إلى وضع CSS (100dvh).
- عند التركيز/اللمس على حقل الكتابة (`#msgInput`, `#pmInput`) يُستدعى `VK.show()` لإظهار الكيبورد داخل إيماءة المستخدم.
- نُنزل الرسائل لآخر رسالة عند فتح/إغلاق الكيبورد، ونُبقي الحقل النشط ظاهراً فوقها.
- للمتصفحات التي لا تدعم VirtualKeyboard API: يبقى `visualViewport` كاحتياط.

**التحقق (في متصفح حقيقي):** `hasVK=true`، `overlaysContent=true`، وعند إطلاق `geometrychange` بارتفاع 320px انخفض ارتفاع الإطار من **844** إلى **524** (`844-320`). ✓

> ملاحظة: هذا يضبط الحجم داخل الموقع. أما **شريط أدوات لوحة المفاتيح** (🎨 ✏️ 😀 في Gboard) فهو من لوحة المفاتيح نفسها ويُخفى من إعدادات Gboard — ليس من الموقع.

---

## ⑥ منع الدخول من جهاز آخر بنفس الحساب المسجّل

**المشكلة:** عند دخول شخص آخر بنفس الحساب المسجّل من جهاز مختلف، كانت الجلستان تعملان معاً.

**الحل:** 
- **`server.js`**: خريطة `USER_ACTIVE_TOKEN` (uid → رمز الدخول الحالي). عند إصدار رمز جديد لحساب مسجّل:
  - يُلغى الرمز القديم ويُحذف من `CHAT_TOKENS`.
  - تُقطع كل اتصالات Socket.IO القديمة لهذا الحساب (`disconnectRegisteredSessions`) مع إرسال حدث `session_conflict`.
  - عند اتصال Socket برمز لم يعد نشطاً (مثلاً إعادة اتصال قديمة) يُرفض ويُعرض القالب.
- **`public/index.html`**: قالب النافذة `sessionConflictOv` (بنمط قالب الحظر) بنص "تم تسجيل الدخول إلى حسابك من جهاز آخر" + زر للعودة لتسجيل الدخول.
- **`public/css/style.css`**: أنماط القالب بلون أزرق مميز.
- **`public/js/app.js`**: دالة `showSessionConflictTemplate()` وحدث `SOCKET.on('session_conflict', ...)` يعرض القالب ويُنهي الجلسة المحلية (مسح الرمز/الهوية وقطع الاتصال).

**النتيجة:** عند دخول شخص بالحساب نفسه من جهاز آخر، تُنهى الجلسة القديمة تلقائياً وتظهر الجلسة القديمة قالب "تم الدخول من جهاز آخر"، بينما تعمل الجلسة الجديدة. (تم التحقق باختبار حقيقي: الجهاز الأول استلم `session_conflict` وانقطع، والجهاز الثاني اتصل بنجاح.)

> ملاحظة: تُطبَّق على **الحسابات المسجّلة** فقط؛ الزوّار (أسماء مؤقتة) لا يتأثرون.

---

## ④ لون شارة العضوية في ملف المستخدم + لون زر قائمة الألوان

**المشكلة:** 
- شارة العضوية/الرتبة في ملف المستخدم (`profile-main-pill`) كانت بلون ثابت لا يتبع الرتبة.
- زر قائمة الألوان (`btnApps`) ظل بلون السمة ولم يعكس اللون المختار/المحفوظ.

**الحل (في `public/js/app.js`):**
1. **`profile-main-pill`** في `renderVisitorProfile` → أصبح نَصّ العضوية يُلوَّن بـ `userColor(u)` (نفس لون اسم المستخدم في العام):
   - سوبر ادمين/ادمن → **أسود** · أدمن غرفة → أحمر · مميّز → زهري · VIP → أزرق · بلس → أخضر · بريميوم → أزرق فاتح · مسجّل → بني · زائر → أسود.
2. **أيقونة `btnApps`** → دالة `applyMyColorToAppsButton()` تُطبّق اللون المختار/المحفوظ (`MY_COLOR`) على أيقونة زر قائمة الألوان، وتُرجعها للون السمة تلقائياً عند اختيار «تلقائي».
   - تُستدعى عند اختيار لون من الشبكة، وعند تحميل اللون من حساب العضو (`syncMyColorFromProfile`)، وعند تسجيل الدخول.

**النتيجة:** كل رتبة/عضوية تظهر بلونها في ملف المستخدم، ولون أيقونة زر قائمة الألوان يعكس اللون المختار والمحفوظ على أي جهاز. (تم التحقق في متصفح حقيقي.)

---

> ⚠️ مهم جداً
- عند تشغيل المشروع لأول مرة سيُضاف عمود `color` تلقائياً (التعديل آمن وقابل للتكرار).
- إذا ظهرت لك نسخة قديمة في المتصفح، اعمل **تحديثاً قسرياً**: `Ctrl+Shift+R` أو `Cmd+Shift+R`.
- سلوك الكيبورد الأنسب على المتصفحات الحديثة؛ على iOS تختلف التفاصيل قليلاً لكن الحقل يبقى ظاهراً.
