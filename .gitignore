# Local/runtime dependencies and generated files
node_modules/
.env

# أيقونات مسارات الأرشفة المولّدة تلقائياً (تُولَّد عند التشغيل من بصمة المسار)
public/uploads/favicons/

# TLS private keys/certificates are supplied by the server (aaPanel/Let's Encrypt)
/cert.pem
/key.pem
/cert.pem.bak
/key.pem.bak
