# إصلاح: تعدّد بثوث الفيديو في نفس الوقت 🎥🎥

## المشكلة
عند فتح بث فيديو كمذيع، ثم دخول مذيع آخر أو الموافقة على طلب مشاهدة بينهما،
كان البث يتوقف عن العمل لدى أحد الطرفين أو كليهما.

## السببان الحقيقيان

### 1) تصادم إشارات WebRTC بين المذيعَين (السبب الرئيسي)
عند تبادل مذيعَين المشاهدة يوجد **اتصالان مستقلان مع نفس الشخص**:
`out:UID` (أرسل له بثي) و `in:UID` (أستقبل بثه).
كانت دالة `bcastGetPeer()` تُرجع أول اتصال تجده (`in` دائماً)، فيُطبَّق
`answer` / `ICE candidate` الخاص باتصال الإرسال على اتصال الاستقبال →
`setRemoteDescription` يفشل وينقطع **الاتصالان معاً** = «البث يوقفني عن العمل».

**الحل:** كل إشارة صارت تحمل اتجاهها لدى مُرسِلها (`dir: 'out' | 'in' | 'both'`)،
والمستقبل يختار الاتصال بالاتجاه المعاكس (`bcastOppositeDir`) عبر
`bcastPeerForSignal()` — فلا تختلط الإشارات أبداً.
كذلك مُنع إعادة الاتصال التلقائي على اتصالات `in` حتى لا تُلغي اتصال الإرسال لنفس الشخص.

### 2) كان النظام يسمح بمشاهدة **مذيع واحد فقط** في نفس الوقت
عند قبول بث جديد كان الخادم يغلق البث المقبول سابقاً تلقائياً
(`viewerOf: Map(viewer -> host)` مذيع واحد لكل مشاهد).

**الحل:** أصبحت العلاقات متعددة ومتزامنة:
- `viewerOf: Map(viewerId -> Set(hostId))`
- `pending:  Map(viewerId -> Map(hostId -> info))`
- **لا يُغلق أي بث مقبول عند قبول بث آخر** — كل بث وافق صاحبه على طلبه يعمل بشكل طبيعي.
- من كان يشاهد بثاً ثم صعد مذيعاً، تبقى مشاهدته شغّالة (`ack.watching` يعيدها للعميل).
- `bcast:leave` و`bcast:watch_cancel` يقبلان الآن `hostId` اختيارياً لإيقاف **بثٍّ واحد** فقط.
- `bcast:watch_response` صار يحمل `hostId` لتمييز صاحب الرد عند تعدد الطلبات.

## الملفات المعدَّلة
| الملف | التعديل |
|---|---|
| `server.js` | بنية علاقات المشاهدة المتعددة + مساعدات `bcastAddWatch/bcastRemoveWatch/bcastPendingMap` + تحديث `watch_request/response/cancel/leave/signal/start` و`removeHostFromBroadcast` و`cleanupBroadcastForUser` |
| `public/js/app.js` | إشارات باتجاه (`dir`) + حالة `watching`/`pendingTargets` بدل `targetHostId` الواحد + عرض عدة بلاطات معاً + إيقاف مشاهدة بثٍّ واحد دون التأثير على البقية |
| `public/index.html` | تحديث نص نافذة تأكيد المشاهدة |
| `test-bcast.js` | تحديث الاختبارات للسلوك الجديد (بثوث متزامنة) + اختبارات جديدة |
| `test-*.js` | دعم `BASE` من متغيّر البيئة وشهادة self-signed |

## نتائج الاختبار الآلي
```
node test-bcast.js   →  33 ناجح / 0 فاشل   (تعدد البثوث والإشارات)
node test-audio.js   →  15 ناجح / 0 فاشل   (البث الصوتي لم يتأثر)
```

## التشغيل
```bash
npm install
DOMAIN=chat-arab.me npm start                         # HTTPS على 2083 عبر شهادة aaPanel
PORT=3000 HTTPS_KEY=/x HTTPS_CERT=/x node server.js   # HTTP على 3000
```
