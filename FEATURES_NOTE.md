# ملاحظات الميزات الجديدة (2026-08-26)

## 7) التحقق من البريد (Gmail) عند التسجيل
- **التسجيل يتطلب بريد Gmail إلزامياً** (ينتهي بـ @gmail.com) + حقلاً جديداً في نموذج التسجيل.
- بعد التسجيل: **يُرسل رمز تفعيل (6 أرقام) إلى الجيميل** — الحساب يبقى غير مُفعَّل ولا يستطيع الدخول إلى الدردشة (الخادم يفصل الاتصال ويعرض شاشة إدخال الرمز) حتى إدخال الرمز الصحيح.
- **البريد فريد**: البريد المستخدم في حساب لا يمكن استخدامه لحساب آخر.
- الرمز صالح 10 دقائق، 5 محاولات إدخال، إعادة إرسال بمعدل (60 ثانية بين الإرسالين، 3 كحد أقصى بالساعة).
- **المستخدمون القدامى (بدون بريد) لا يتأثرون** — يدخلون بشكل طبيعي.
- **إدارة SMTP من لوحة الإدارة** (الاعدادات الاساسية ← البريد الإلكتروني والتحقق): تفعيل SMTP، host/port/user/pass/SSL/from + زر بريد تجريبي + **سجل الرسائل** (البريد/الموضوع/الرمز/الحالة/الخطأ).
  - لمعاينة Gmail: `smtp.gmail.com` — منفذ 587 (SSL غير مفعّل) أو 465 (SSL مفعّل) — مع «كلمة مرور تطبيق» من Google.
  - قبل تفعيل SMTP: يُسجل الرمز في القاعدة ويظهر في سجل الرسائل (وضع التطوير).

## 8) تسكير الهدايا بالكمية (هدايا متكررة) + تحقق رقم الحساب
- **الهدايا المتكررة تُعرض مجمّعة**: «وردة ×100 (5 ذهب = 500)» مع **عدّاد كمية (− / +)** لاختيار العدد المطلوب تسكيه من كل نوع.
- **يُخصم فقط المحدد**: إن كانت لديها 100 وردة وطلبت تسكير 20 → تُحذف الـ 20 وتبقى 80. وإن كان السطر نفسه بكمية 10 واختارت 4 → يبقى 6 في نفس السطر.
- **تحقق رقم الحساب المستلم**: أرقام فقط (8-19 خانة) — يُرفض أي بريد بأحرف أو أقصر.
- لوحة الإدارة تعرض **تفاصيل الهدايا المحددة** (اسم × كمية) في بطاقة الطلب.

## 5) مكالمة فيديو احترافية: تكبير/تصغير + إشعارات الكاميرا
- **نقر على صورة أي طرف = تبديل**: النقر على صورة صورك المصغرة (PiP) يكبّرك ملء الشاشة ويصغّر المقابل، والنقر مرة أخرى (أو على صورة المقابل) يعيد الوضع — بحركة انسيابية.
- **إشعارات الكاميرا**: عندما يغلق الطرف الآخر كاميرته يظهر فوق الفيديو إشعار «اسمه أغلق الكاميرا» (وتوست)، وعند فتحها «اسمه فتح الكاميرا» — عبر حدث `call:cam_state`.
- التسجيل (إن وُجد) يتبع التكوين نفسه: الكبير/المصغر في التسجيل يطابق ما تراه الشاشة.

## 6) تسكير الهدايا: اختيار الهدايا (يُحذف المحدد فقط)
- عند طلب التسكير: **خطوة اختيار** تعرض كل هداياها (سطراً سطراً: الصورة/الاسم/الكمية/قيمة الذهب) مع اختيار متعدد وشريط «المحددة: X 🪙 / المطلوب Y 🪙» — الزر لا يتفعّل إلا إذا بلغ مجموع المحدد الحد المطلوب.
- **خطوة الحساب**: ملخص (N هدية / X ذهب ← $Y) + بيانات الحساب البنكي + إرسال.
- عند «اتمام التحويل» من الإدارة: **تُحذف الهدايا المحددة فقط** من حسابها وتبقى بقية هداياها كما هي (تستطيع التسكير مجدداً عند بلوغ الحد).
- الطلب المُرفوض لا يحذف أي هدية.
- لوحة الإدارة تعرض: عدد الهدايا المحددة + ذهبها + مبلغ التسكير + بيانات الحساب.


## 1) جميع الغرف أصبحت صوتية (حذف تبويبَي «الافتراضية / الصوتية»)
- حُذف شريط التبويبات من أعلى قائمة الغرف ومن لوحة الغرف داخل الدردشة.
- كل الغرف تُعرض في قائمة واحدة دون تقسيم.
- على مستوى القاعدة: ترقية تلقائية تحوّل كل الغرف الحالية إلى `type='voice'`، وأي غرفة جديدة (إضافة/تعديل من لوحة الإدارة) تُحفَظ دائماً كنوع «صوتية» — اختفي اختيار نوع الغرفة من نموذج الإضافة.
- البث في الغرف يعمل صوتياً كما في الغرف الصوتية سابقاً (يُسمع الجميع تلقائياً).

## 2) مكالمة فيديو خاصة بأسلوب سناب شات
- زر جديد (كاميرا خضراء) بجانب زر الهاتف في هيدر المحادثة الخاصة.
- شاشة مكالمة كاملة: فيديو الطرف الآخر ملء الشاشة + نافذة مصغّرة (PiP) لكاميرتك + أزرار (كتم / إنهاء / الكاميرا) + زر تصغير لشريط عائم.
- قبل وصول الفيديو تظهر بطاقة انتظار بالصورة واسم الطرف (نفس روح سناب شات).
- **تحديد من يمكنه الاتصال**: مفتاح جديد `video_call_allowed_memberships` في «صلاحيات العضويات» (الافتراضي: مميز/بلس/بريميوم/VIP) — الإدارة تحدد بدقة أي العضويات تستطيع بدء مكالمة فيديو.
- **تحديد العملة/التكلفة**: مفتاح جديد `video_call_cost` (ذهب) في «تكاليف العضويات والمكالمات» — يُخصم من المتصل (المُبادر) عند قبول المكالمة. التجربة المجانية (دقيقة أولى) للمكالمات الصوتية فقط.
- الإدارة (admin/superadmin/supermaster/roomadmin) تتصل مجاناً.

## 3) نظام تسكير الهدايا (ذهب الهدايا ← مبلغ دولار) — للفتيات فقط
**الفكرة:** الهدايا بقيمها **بالذهب فقط** (لا يوجد سعر دولار لكل هدية). الإدارة تحدد:
- **مجموع الذهب المطلوب للتسكير** (مثال: 100 ذهب) — `cashout_gold_min`
- **المبلغ بالدولار الذي تدفعه عند بلوغه** (مثال: 5$) — `cashout_usd_amount`
- **حساب الإدارة المصدر** (يُسحب منه ويدفع لمستلمة الهدايا يدوياً) — `cashout_source_account`
- تفعيل/تعطيل النظام — `cashout_enabled`

من: **لوحة الإدارة ← الهدايا والإيموجي ← تسكير الهدايا**.

**المستخدمة (فتاة مسجلة) — من «هدايا حسابي»:**
- بطاقة تسكير تعرض: عدد الهدايا، **مجموع ذهبها 🪙**، المجموع المطلوب، والمبلغ الذي سيُحوَّل لها ($)، مع شريط تقدم.
- عندما يصل مجموع ذهب هداياها المستلمة إلى الحد (100 مثلاً): إدخال رقم الحساب البنكي/البطاقة وإرسال الطلب.
- قبل بلوغ المجموع يظهر: «ينقصك X 🪙».

**سير العملية:**
1. الفتاة تُرسل طلب التسكير (الحالة: قيد المراجعة) ويصل تنبيه للإدارة: «اسم — 110 ذهب ← 5$».
2. الإدارة من صفحة «تسكير الهدايا»: تراجع الطلب (الحساب، مجموع الذهب، المبلغ).
3. عند «اتمام التحويل»: الإدارة تدفع المبلغ يدوياً من حسابها، فيُشعَّر المستلمة **ويُحذف رصيد الهدايا من حسابها نهائياً** (تبدأ من جديد).
4. أو «رفض الطلب» مع سبب (يصل إشعار بالسبب إلى المستلمة).
- الطلب المكرر ممنوع ما دام يوجد طلب قيد المراجعة، والميزة خفية تماماً عن غير الفتيات.
- ملاحظة: مجموع الذهب يُحتسب من `price × qty` لهدايا `gifts_log` (قيمة الهدية بالذهب نفسها التي يدفعها المُرسِل).

## 4) حفظ تسجيلات مكالمات الفيديو في الإدارة (سوبر ماستر فقط)
- أثناء مكالمة الفيديو الخاصة يُسجَّل تلقائياً (من جانب المتصل): فيديو المتصل كاملاً + كاميرا المتصل مصغّرة (PiP بأسلوب سناب شات) + صوت الطرفين، في ملف `webm`.
- عند إنهاء المكالمة يُرفع التسجيل تلقائياً ويُحفظ في `public/uploads/calls/` مع سجل في جدول `call_recordings` (الحقول: `call_type` = video/audio و`video_path`).
- عرض/تحميل/حذف: **لوحة الإدارة ← مراقبة الخاص والمكالمات ← تسجيلات المكالمات** — الصفحة **خاصة بالسوبر ماستر (مالك الدردشة) فقط** (أي سوبر أدمن آخر يحصل على 403).
- بطاقة التسجيل: أيقونة/شارة «📹 فيديو» + مشغل فيديو مدمج + تحميل + حذف (يُحذف الملف والسجل معاً).
- تسجيلات الصوت القديمة تستمر بالعمل كما هي (شارة «🎙 صوت»).

## اختبارات
- `test-cashout.js` — 24 فحصاً لنظام التسكير بالنموذج الجديد (API + قاعدة البيانات).
- `test-videocall.js` — 12 فحصاً لمسار مكالمة الفيديو (صلاحيات/رصيد/رنين/خصم/إنهاء + عدم تأثر الصوت).
- `test-bcast-regression.js` — تراجعي: بث الصوت في الغرف بعد تحويلها كلها صوتية.
- `test-recording.js` — 12 فحصاً لخط أنابيب تسجيلات الفيديو (رفع/عرض/حظر غير المالك/حذف الملف والسجل).
- `test-camstate.js` — 4 فحوصات لإشارات كاميرا مكالمة الفيديو (فتح/إغلاق بين الطرفين).
- `test-email.js` — 18 فحصاً لنظام التحقق من البريد (Gmail): التفعيل/الرمز/الفريدة/الحدود.
- ملاحظة: الاختبارات تقيَّد بمعدلات التسجيل/الدخول (6 تسجيلات/5 دقائق، 8 دخولات/دقيقة) — لذلك تنشئ مستخدميها مباشرة في القاعدة عند الحاجة.

> ملاحظة: القيم المحفوظة حالياً في الإعدادات: مفعّل، 100 ذهب ← 5$ — عدّلها من لوحة الإدارة حسب احتياجك.
